// Optional global test setup
require('dotenv').config(); // Load environment variables during tests

// You can add global mocks, configurations, or setup here
beforeAll(() => {
  // Global setup code if needed
});

afterAll(() => {
  // Global teardown code if needed
});